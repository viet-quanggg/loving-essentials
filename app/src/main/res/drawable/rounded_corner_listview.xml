<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />
    <corners android:radius="12dp" />
    <shadow android:color="@android:color/darker_gray"
        android:radius="4dp" />
</shape>
